# Benchmark OG Card

`og-card.html` renders the scoring table at 1200×630 for social sharing.

Same generation + deployment process as `04-deliverables/demo-site/og/README.md`.

The card is intentionally information-dense (the table is the argument). For X/Twitter card preview, the table fits cleanly in `summary_large_image` aspect ratio.
